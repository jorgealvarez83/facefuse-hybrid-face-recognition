# Runtime dependencies for FaceFuse.
# Equivalent to `pip install -e .`; kept for environments that prefer a
# requirements file. For GPU inference also install onnxruntime-gpu.

numpy>=1.24
opencv-python>=4.8,<5   # OpenCV 5 removed cv2.HOGDescriptor
scikit-learn>=1.3
matplotlib>=3.7

# Face detection (YOLO11 backbone)
torch>=2.0
ultralytics>=8.3

# 512-d face embeddings
insightface>=0.7
onnxruntime>=1.16
